# Results

本目录只保存未来从 exact installed public path 产生的 v0.4 Agent Eval 结果。当前预注册提交尚未运行 Agent，因此没有结果文件；状态必须保持 `NOT_RUN`。

结果只能在九个匿名 workspace、九个独立 Reviewer attempt 和 evaluator-side runtime checkpoints 全部落盘后追加。不得回写或修改 `cases/`、`evaluator/expected.json`、`evaluator/preregistration.json` 或既有结果来迎合门槛。
