# Route Select — Controller Instructions v0.1

This internal module is not directly activatable. The Host Agent has already submitted an exact route destination through Signal Intake. The deterministic Controller may validate that enum and map it to the corresponding legal next state; it must not inspect keywords or replace the Agent's classification.

Reject missing provenance, unsupported destinations, stale attempts, or a destination inconsistent with the current state. Keep `existing_links` as metadata and never treat them as an automatic route override.
